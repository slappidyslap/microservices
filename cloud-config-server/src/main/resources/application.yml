spring:
  application:
    name: cloud-config-server
  
  cloud:
    config:
      server:
        git:
          uri: file:///D:/JvmProjects/git-localconfig-repo
          
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    
server:
  port: 8888